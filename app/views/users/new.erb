
<h1> Signup for Book Writer </h1>

<p>
  Usernames must be unique, and contain 5 to 32 characters.
  <br>
  Passwords must contain between 8 and 64 characters.
  <br>
  Valid characters are any letter (either case), number, or the punctuation characters below.
  <br>
  ! @ $ % ^ * + _
</p>

<form action="/signup" method="POST">
  <fieldset>
    <legend> Sign Up: </legend>

    <% if flash.has?(:message) %>
      <div id="signup error">
        <h2> <%= flash[:message] %> </h2>
      </div>
    <% end %>

    <br>
    <label for="username"> Username: </label>
    <br>
    <input type="text" name="user[username]" id="username" maxlength="32" pattern="[a-zA-Z0-9!@$%^*+_]{5,32}" required>
    <br>

    <br>
    <label for="password"> Password: </label>
    <br>
    <input type="password" name="user[password]" id="password" maxlength="64" pattern="[a-zA-Z0-9!@$%^*+_]{8,64}" required>
    <br>

    <br>
    <input type="submit" value="Sign Up">
  </fieldset>
</form>
